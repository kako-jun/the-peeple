# DESIGN.md — The Peeple

## 1. Vision

**男子トイレの暗黙ルールを戦略ゲームに昇華した「立ち位置パズル」**。

- プレイヤーは整列係として、次々やってくる客を最適な便器に誘導する
- 「隣を空ける」「端から埋める」という男子トイレの暗黙知がスコアに直結する
- ユーモラスな題材でありながら、待機列管理と先読みが要求される本格パズル
- サブタイトル **Stand Off**（立ち位置の対決・対峙のダブルミーニング）

---

## 2. Design Tokens

### 2-1. カラーパレット

#### UI カラー

```
UI_BG            = #0f0f1a  // アプリ背景（深い紺）
UI_PRIMARY       = #7c3aed  // メインアクション（Violet）
UI_SECONDARY     = #06b6d4  // ホバー・選択中のハイライト（Cyan）
UI_TEXT_PRIMARY  = #ffffff  // 本文テキスト
UI_TEXT_DIM      = #aaaaaa  // 補助テキスト・ラベル
```

#### 便器カラー

```
URINAL_EMPTY     = #4a9eff  // 空き（青）
URINAL_OCCUPIED  = #e74c3c  // 使用中（赤）
```

#### 客タイプ別カラー

```
CHAR_NORMAL      = #3498db  // 普通の客（青）
CHAR_RUSHER      = #e67e22  // 急ぎ客（オレンジ）
CHAR_GROUP       = #27ae60  // 団体客（緑）
CHAR_DRUNK       = #9b59b6  // 酔っぱらい（紫）
```

> **Do**: 色 + ラベル文字（N / R! / G / ?）で二重に区別する。色覚多様性ユーザーへの配慮。

#### 苛立ちゲージカラー

```
ANGER_LOW        = #ffcc00  // anger < 50%（黄）
ANGER_MID        = #ff8800  // anger 50〜80%（オレンジ）
ANGER_HIGH       = #ff2222  // anger > 80%（赤）
```

#### 入口・施設カラー

```
ENTRANCE_COLOR   = #27ae60  // 入口マーカー（緑）
QUEUE_LINE       = #aaaaaa  // 待機ライン（点線、alpha 0.3）
```

### 2-2. タイポグラフィ

| 用途                                 | フォント          | サイズ            | 備考                       |
| ------------------------------------ | ----------------- | ----------------- | -------------------------- |
| ゲームロゴ「The Peeple」             | Inter / system-ui | 44px bold 700     | タイトル画面               |
| 副題「Stand Off」                    | Inter / system-ui | 16px normal       | alpha 0.5                  |
| HUD（時間・スコア・ミス）            | Inter / system-ui | 14〜16px bold 600 | 白                         |
| ボタンラベル                         | Inter / system-ui | 14〜16px bold 600 | 白                         |
| セクションラベル（モード・難易度）   | Inter / system-ui | 11px medium 500   | #888888                    |
| 便器ラベル「タップ」                 | Inter / system-ui | 10px              | 白、空きのみ表示           |
| 便器番号（1〜4）                     | Inter / system-ui | 10px              | 白、常時表示（#26 未実装） |
| 客タイプイニシャル（N / R! / G / ?） | Inter / system-ui | 10px              | 白（#26 未実装）           |
| 結果画面サマリ                       | Inter / system-ui | 12〜16px          | 白・薄灰                   |
| 総評テキスト                         | Inter / system-ui | 13px              | #ffd700（金色）            |

---

## 3. Layout — 誌面座標系

`SceneManager` は全シーンを一枚の誌面コンテナ（`world`）上に配置し、カメラを tween して遷移する。

```
誌面 Y 軸（px）
        0 ─────────────────── title 中心 (VIEW_H/2 = 320)
      640 ─────────────────── play 中心 (VIEW_H*1.5 = 960)
     1280 ─────────────────── result 中心 (VIEW_H*2.5 = 1600)

      ※ lineRush は play と同じ Y 位置 (960) に配置。
         Stand Off の play エリアを共有し、同モードとして扱う。
```

各シーンの `x` は誌面ローカルで `VIEW_W / 2 = 180`（センタリング）。

カメラ遷移: `cubicInOut` で 800ms（ゲーム内遷移）または 0ms（即スナップ）。

---

## 4. Layout — PlayScene (360×640、ピクセル座標: 中央 = 0,0)

### 4-1. 全体構成

```
PlayScene 中央原点 (0, 0) を中心として描画。
Container の (x, y) は world 上の絶対値 (VIEW_W/2, VIEW_H*1.5)。

 ─────────────────────── y = -320 (画面上端)
  HUD (苛立ちゲージ背景)  y = -312〜-304
  時間 [左]  SCORE [中]  MISS [右]   y = -312
  苛立ちゲージ (幅160px)  y = -288〜-280
 ─────────────────────── y = -288 (ゲージ下)

  入口マーカー (40×28px)  x = -160, y = -260
  ↓（ENTERING 移動）
  待機列先頭 (中央縦線)   x = 0,    y = -40
  待機列 2番目以降         y = -40 - (index × 36)
  （QUEUE_SPACING = 36px）

  ─────── キャラ(半径10px) が移動 ───────

  便器列 (横一列、4基)    y = VIEW_H/2 - 80 - uH/2 = 190 (基準)
  便器幅 44px, 高さ 60px, 間隔 12px, 全体幅 212px
  id:0  id:1  id:2  id:3
  x: -106  -50   6   62  (startX = -106, step = 56)
  「タップ」ラベル: 便器 y + 40

 ─────────────────────── y = +320 (画面下端)
```

### 4-2. レイヤー構成

```
PlayScene (Container)
  └── urinalLayer   便器 Graphics + ラベル Text
  └── charLayer     キャラ Graphics (circle) + anger バー Graphics
  └── hudLayer      時間 / スコア / ミス Text + 苛立ちゲージ
  └── 入口 Graphics + Text  (addChild 直接)
  └── 待機ライン Graphics    (addChild 直接)
```

### 4-3. HUD 詳細（y = -312〜-280）

| 要素             | 位置                      | 内容                                       |
| ---------------- | ------------------------- | ------------------------------------------ |
| 残り時間         | 左端 x=-170, anchor=(0,0) | `{N}s` 形式、カウントダウン                |
| スコア           | 中央 x=0, anchor=(0.5,0)  | `SCORE: {N}`                               |
| ミス             | 右端 x=170, anchor=(1,0)  | `MISS: {N}/{MAX_MISSES}` 残り1でテキスト赤 |
| 苛立ちゲージ背景 | x=-80〜+80, y=-288, h=8px | #333333 alpha 0.8                          |
| 苛立ちゲージ本体 | anger 比率で伸縮          | 黄→オレンジ→赤                             |
| 苛立ちラベル     | x=-80, y=-278             | 「苛立ち」#aaaaaa 10px                     |

### 4-4. キャラ描画

| 状態               | 色                  | 備考         |
| ------------------ | ------------------- | ------------ |
| ENTERING / QUEUING | タイプ別カラー      | 移動中       |
| APPROACHING        | タイプ別カラー      | 便器へ向かう |
| USING              | `#f39c12`（黄色）   | 使用中       |
| LEAVING            | `#95a5a6`（グレー） | 退室中       |

- 円半径: `CHAR_R = 10px`
- 苛立ちバー: 頭上 `y - CHAR_R - 5`、幅 `CHAR_R × 2`、高さ 3px、QUEUING 中のみ表示
- 客タイプラベル: 円内にイニシャル（**#26 未実装**）
- 使用中残り時間バー: 頭上に水平バー（**#27 未実装**）

### 4-5. 便器描画

| 状態     | 塗り色                    | ラベル           |
| -------- | ------------------------- | ---------------- |
| EMPTY    | `#4a9eff`（青）alpha 0.85 | 「タップ」y + 40 |
| OCCUPIED | `#e74c3c`（赤）alpha 0.85 | なし             |

- 枠線: 白 alpha 0.4、幅 1.5px、角丸 6px
- 便器番号（1〜4）常時表示: **#26 未実装**

---

## 5. Layout — TitleScene (360×640)

```
y = -200  ロゴ「The Peeple」 (44px, anchor 中央)
y = -150  副題「Stand Off」  (16px, alpha 0.5)

y = -100  セクションラベル「モード」
y = -68   モード選択ボタン 2本横並び (140×48px、間隔 12px)
            Stand Off (選択可) | Line Rush\n(coming soon) (disabled)

y = -8    セクションラベル「難易度」
y = +28   難易度ボタン 2本横並び (140×48px)
            NORMAL | HARD

y = +104  スタートボタン (144×52px、中央)
```

### トグルボタン視覚状態

| 状態                   | 塗り alpha | 枠線色              | 枠線 alpha   |
| ---------------------- | ---------- | ------------------- | ------------ |
| 選択中                 | 0.4        | UI_SECONDARY (Cyan) | 1.0、幅 2px  |
| ホバー（未選択）       | 0.22       | UI_SECONDARY        | 0.8、幅 1px  |
| 通常（未選択）         | 0.12       | UI_PRIMARY (Violet) | 0.45、幅 1px |
| disabled (Coming Soon) | 0.05       | #444444             | 0.3、幅 1px  |

### スタートボタン

| 状態   | 塗り alpha | 枠線             |
| ------ | ---------- | ---------------- |
| 通常   | 0.2        | Violet alpha 0.5 |
| ホバー | 0.35       | Cyan alpha 0.9   |

---

## 6. Layout — ResultScene (360×640)

```
y = -270  見出し「ゲームオーバー」or「タイム終了！」(36px bold)
y = -200  サマリコンテナ原点
   +0     SCORE / MISS / TIME 横並び (16px bold)
   +28    総評テキスト (#ffd700 金色、13px)
   +56    「── 適用ルール ──」(12px グレー)
   +76〜  ルール一覧（最大5件、各 18px）
            + ルール名 × 回数  (+スコア) — 緑/赤色分け

y = +170  「もう一度 (R)」ボタン (220×52px)
y = +238  「タイトルへ (Esc)」ボタン (220×52px)
```

### 総評テキスト分岐ロジック

| 条件               | テキスト                           |
| ------------------ | ---------------------------------- |
| ミス=0、スコア≥100 | 完璧な誘導！礼儀正しさの達人       |
| ミス=0             | 誰も怒らせなかった。紳士的です     |
| スコア≥80          | なかなかの手腕。あと少しで達人     |
| ミス≥4             | かなり怒らせてしまった…修行が必要  |
| スコア≥40          | 平均的な誘導。もう少し端を意識して |
| それ以外           | まだまだ練習が必要です             |

---

## 7. Layout — LineRushScene (360×640)

現在 Coming Soon プレースホルダのみ（**#31 未実装**）。

```
y = -160  「LINE RUSH」(40px bold)
y = -80   「Coming Soon」(20px, alpha 0.6)
y = +20   説明文 3行 (13px, alpha 0.4)
y = +120  「← タイトルに戻る」ボタン (220×52px)
```

---

## 8. ゲームルール詳細

### ゲーム終了条件

| 条件                        | 種別                             |
| --------------------------- | -------------------------------- |
| ミス ≥ 5（`MAX_MISSES`）    | ゲームオーバー                   |
| 経過時間 ≥ 90,000ms（90秒） | タイム終了（クリア扱い）         |
| Esc キー                    | ギブアップ（ゲームオーバー扱い） |

### ミス発生条件

| 条件                         | 説明                                   |
| ---------------------------- | -------------------------------------- |
| anger ≥ 100                  | 苛立ちゲージ満タンで客が怒って退場     |
| 待機列 ≥ 6 人（`QUEUE_MAX`） | 行列パンクで入場不可の客がミス扱い退場 |

### 心理スコアリングルール

| ルール ID           | 発動条件                         | スコア                             |
| ------------------- | -------------------------------- | ---------------------------------- |
| `END_URINAL`        | 端の便器（id=0 or id=3）を使用   | +10                                |
| `NO_NEIGHBOR`       | 左右に誰も占有していない         | +20                                |
| `NEIGHBOR_EMPTY`    | 1つ空けた使用（距離≥2）          | +5（**#32 要便器数増加で有効化**） |
| `SAME_COLUMN_TABOO` | 左右いずれかが占有状態           | -15                                |
| `FORCED_ADJACENT`   | 両隣が全部埋まっていて選択肢なし | ±0                                 |

> **注意**: 現状 `NEIGHBOR_EMPTY` は4台構成で発動しない（distance=1しかないため）。#32 で5台以上に増やすと有効化される。

### 客タイプ詳細

| タイプ | angerRate (anger/ms) | useDuration   | speedMult | 解放タイミング (NORMAL)    |
| ------ | -------------------- | ------------- | --------- | -------------------------- |
| NORMAL | 0.004                | 5000〜10000ms | 1.0       | ゲーム開始から             |
| RUSHER | 0.012（3×）          | 2000〜4000ms  | 1.8       | 20秒後                     |
| GROUP  | 0.003                | 6000〜12000ms | 0.9       | 40秒後                     |
| DRUNK  | 0.006                | 8000〜14000ms | 0.7       | 40秒後（ランダム便器突撃） |

### スポーン間隔

| 難易度 | スポーン間隔        |
| ------ | ------------------- |
| NORMAL | 3000ms（3秒ごと）   |
| HARD   | 1800ms（1.8秒ごと） |

HARD はさらに序盤から RUSHER / DRUNK の出現率も高い（10秒以内に30%の確率で RUSHER）。

---

## 9. シーン遷移フロー

```
TitleScene
  ├─ Stand Off 選択 + スタート → [カメラ tween 800ms] → PlayScene (再生成)
  └─ Line Rush 選択 + スタート → [カメラ tween 800ms] → LineRushScene

PlayScene
  ├─ ゲームオーバー / タイム終了 → [800ms] → ResultScene (kind: 'clear' or 'gameover')
  └─ Esc ギブアップ             → [800ms] → ResultScene (kind: 'gameover')

LineRushScene
  └─ ← タイトル / Esc / Enter → [800ms] → TitleScene

ResultScene
  ├─ R / Enter / 「もう一度」 → 直前の difficulty で PlayScene 再生成
  └─ Esc / 「タイトルへ」   → [800ms] → TitleScene
```

---

## 10. Do / Don't

| Do                                                  | Don't                          |
| --------------------------------------------------- | ------------------------------ |
| 便器・客を色 + ラベルで二重区別する                 | 色だけでタイプを判断させる     |
| 苛立ちゲージで「あと何秒か」を感じさせる            | 突然怒って退場する（予告なし） |
| 待機列の人数を HUD に常時表示する                   | 何人並んでいるか分からない UI  |
| 端を選ぶと「+10」と視覚フィードバックを出す（将来） | スコアが増えた理由が伝わらない |
| Result でルール一覧を教育的に見せる                 | なぜそのスコアなのか伏せる     |
| QUEUE_MAX に近づいたら警告色にする（将来）          | 突然パンクする                 |

---

## 11. Roadmap

| Phase       | 内容                                          | 対応 Issue      |
| ----------- | --------------------------------------------- | --------------- |
| ✅ Phase 0  | PixiJS 雛形・シーン遷移                       | #9 #10          |
| ✅ Phase 1a | 便器配置・キャラスポーン/誘導タップ           | #11 #12         |
| ✅ Phase 1b | 状態遷移・スコアリング・HUD・客バリエーション | #13 #14 #15 #16 |
| ✅ Phase 1c | 結果画面・ゲームオーバー・ギブアップ          | #17             |
| ✅ Phase 2a | タイトル + モード/難易度選択                  | #18             |
| ✅ Phase 2b | Line Rush シーン stub + SceneManager 配線     | #19             |
| 🔲 Phase 3a | 便器番号表示 + 客タイプラベル（視認性）       | #26             |
| 🔲 Phase 3b | GROUP 隣接ペナルティ免除（バグ修正相当）      | #25             |
| 🔲 Phase 3c | 使用中キャラの残り時間インジケーター          | #27             |
| 🔲 Phase 3d | 便器5台化 + NEIGHBOR_EMPTY 有効化             | #32             |
| 🔲 Phase 4a | URL クエリ直接起動（デバッグ基盤）            | #30             |
| 🔲 Phase 4b | ハイスコード保存 + NEW RECORD 表示            | #29             |
| 🔲 Phase 4c | SE / BGM 実装                                 | #28             |
| 🔲 Phase 5  | Line Rush 本実装（仕様確定後）                | #31             |
| 🔲 Phase 6  | ドット絵素材・キャラデザイン本実装            | 新規起票        |
